# Line Tracker Controller

## `LT_IsInside(void) : int16_t`

**Description** : Polls sensors and returns a int16_t value representing a sensor hit or not.
### Use:
```c++
int16_t sensors = LT_IsInside();
```
